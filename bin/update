#!/usr/bin/env ruby
require 'pathname'
require 'fileutils'

# path to your application root.
APP_ROOT = Pathname.new File.expand_path('../../', __FILE__)

def system!(*args)
  system(*args) || abort("\n== Command #{args} failed ==")
end

# Disable installation of development gems if desired
no_dev_gems_argument = ENV['NO_DEV_GEMS'] ? '--without development' : ''

FileUtils.chdir APP_ROOT do
  puts '== Installing dependencies =='
  system('bin/bundle check') || system!("bin/bundle install -j4 #{no_dev_gems_argument}")
  system('bin/bundle-detached check') || system!("bin/bundle-detached install -j4 #{no_dev_gems_argument}")

  puts "\n== Updating NPM packages =="
  system! 'yarn install'

  puts "\n== Updating database =="
  system! 'bin/rails db:migrate'

  puts "\n== Removing old logs and tempfiles =="
  system! 'bin/rails log:clear tmp:clear'

  puts "\nNow restart Rails and gulp watcher!"
end
