<!DOCTYPE html>
<html>
  <head>
    <title>Amelia</title>
    <%= csrf_meta_tags %>
    <%= frontend_app_asset_tags "#{params[:controller]}/#{params[:action]}" %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
