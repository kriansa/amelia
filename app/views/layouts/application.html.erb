<!DOCTYPE html>
<html>
  <head>
    <title><%= app_title %></title>
    <%= csrf_meta_tags %>
    <%= frontend_app_asset_tags "#{params[:controller]}/#{params[:action]}" %>
  </head>

  <body>
    <%== flash_messages %>

    <%= yield %>
  </body>
</html>
