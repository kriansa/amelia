<!DOCTYPE html>
<html>
  <head>
    <title>Amelia</title>
    <%= csrf_meta_tags %>
    <%= frontend_app_asset_tags "#{params[:controller]}/#{params[:action]}" %>
  </head>

  <body>
    <p class="notice"><%= flash[:notice] %></p>
    <p class="alert"><%= flash[:alert] %></p>

    <%= yield %>
  </body>
</html>
