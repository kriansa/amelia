In production environment, SLL certs should be placed
inside this folder with the following names:

* app-cert.crt
* app-cert.key

This is highly recommended so in case the network your
server gets compromised, a MITM attack from within
your infrastructure is not possible.
