node_modules/**
vendor/bundle/**

# Output folder, matches with config/assets.config.js
public/assets

# Test coverage output
coverage

# The following styles has been taken from another author, which is not using
# any of our linter settings, so it's better to disable it
app/assets/stylesheets/paper-dashboard-theme/paper/**/*.scss
doc/paper-dashboard-theme
doc/themify-icons
